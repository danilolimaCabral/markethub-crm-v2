/**
 * Página de Gerenciamento de Credenciais de Marketplace
 * Apenas para Admin Master
 */

import MarketplaceCredentialsManager from '@/components/MarketplaceCredentialsManager';

export default function MarketplaceCredentials() {
  return (
    <div className="container mx-auto py-6">
      <MarketplaceCredentialsManager />
    </div>
  );
}
